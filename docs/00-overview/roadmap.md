# Roadmap

**Status:** Living document · **Cập nhật:** 2026-05-15

> Quy tắc: mỗi phase chỉ "Done" khi đạt **Exit criteria** của nó. Không nhảy phase. Việc nào không thuộc phase hiện tại → ghi vào backlog của phase sau, **không làm xen**. Ước lượng tính cho team 2–4 dev.

## Tổng quan các phase

| Phase | Tên | Mục tiêu | Ước lượng |
|---|---|---|---|
| 0 | Nền tảng | Skeleton, hạ tầng, multi-tenant, khung connector | 2–3 tuần |
| 1 | TikTok — Đồng bộ đơn | Kết nối shop TikTok, đơn tự về, trạng thái chuẩn | 3–5 tuần |
| 2 | Đơn thủ công + SKU + Tồn kho lõi | Master SKU, ghép SKU, trừ/đẩy tồn | 3–4 tuần |
| 3 | Giao hàng & in ấn (TikTok) | Vận đơn, in hàng loạt, picking/packing, scan-to-pack, ĐVVC đợt 1 | 4–6 tuần |
| 4 | Shopee + Lazada | Slot 2 sàn vào sau khi có API | 6–10 tuần |
| 5 | WMS đầy đủ + Đăng bán đa sàn | Nhập/xuất/chuyển kho, kiểm kê, giá vốn FIFO, mass listing, ĐVVC đợt 2 | 8–12 tuần |
| 6 | Tài chính + Mua hàng + Báo cáo + Billing | Settlement, lợi nhuận, PO/NCC, báo cáo, gói thuê bao | 8–12 tuần |
| 7 | **Kế toán đầy đủ (VAS TT133)** | Double-entry GL, CoA, kỳ kế toán + lock, AR/AP, quỹ&NH, VAT + BCTC + Export MISA | 14–22 tuần |
| 8+ | Hậu mãi & nâng cao | Trả hàng/hoàn, chat hợp nhất, **HĐĐT (TT78)**, tối ưu hiệu năng, PWA quét hàng | liên tục |

**Mốc lớn:** dùng được nội bộ ~hết Phase 3 (**~3–4 tháng**) · ra mắt 3 sàn ~hết Phase 5 (**~8–12 tháng**) · tiệm cận full BigSeller ~hết Phase 6 (**~12–14 tháng**) · kế toán chuyên nghiệp ~hết Phase 7 (**~18–22 tháng**).

---

## Phase 0 — Nền tảng  ◑ (code gần xong; còn việc ngoài-code)
**Việc:** mono repo Laravel 11 + React (Vite) embedded · routing `/api/v1/*` + `/webhook/*` + `/oauth/{provider}/callback` + catch-all → SPA · Docker Compose (app, worker, postgres, redis, minio, gotenberg, horizon, mailhog) · Sanctum SPA auth · multi-tenant + RBAC + sub-account khung · khung module + `ChannelRegistry`/`CarrierRegistry` + interface `ChannelConnector`/`CarrierConnector` + DTO chuẩn · migration nền + cơ chế partition theo tháng · Horizon supervisors · Sentry + logging · SPA shell (layout AntD, router, auth flow, trang Dashboard/Gian hàng rỗng) · CI lint+test · **(song song, không code) nộp hồ sơ Shopee + Lazada Open Platform, đăng ký app TikTok Shop Partner**.
**Exit criteria:** đăng ký/đăng nhập được, tạo tenant + mời thành viên, SPA chạy, queue chạy, CI xanh, Docker `up` là chạy được toàn bộ. Hồ sơ 2 sàn đã nộp.

**Trạng thái (2026-05-11):**
- ✅ Mono repo + routing 4 nhóm (`/api/v1`, `/webhook`, `/oauth/{provider}/callback`, catch-all → SPA).
- ✅ Sanctum SPA auth · multi-tenant + global scope + `BelongsToTenant` · RBAC (Role enum + permission map + `Gate::before`) · sub-account scope cột sẵn · audit log.
- ✅ Khung 11 module (service provider mỗi module) · `ChannelRegistry`/`CarrierRegistry` + interface `ChannelConnector`/`CarrierConnector` + DTO chuẩn + `ManualConnector`.
- ✅ Horizon supervisors (config theo `07-infra/queues-and-scheduler.md`) · scheduler skeleton (`routes/console.php`) · `db:partitions:ensure` + helper `MonthlyPartition`/`PartitionRegistry`.
- ✅ Sentry (web + queue) · log JSON ra stdout + middleware `request_id`/`trace_id` (vào log context, Sentry tag, header `X-Request-Id`, error envelope) · `GET /api/v1/health` (DB/cache/Redis/queue).
- ✅ SPA shell (layout AntD, router, auth flow, Dashboard, Gian hàng, **Cài đặt → Thành viên & phân quyền** với mời/đổi vai trò).
- ✅ CI GitHub Actions: backend (Pint · PHPStan/Larastan level 5 + baseline · migrate · PHPUnit `--coverage --min=60`, hiện ~72%) + frontend (ESLint · typecheck · build); workflow CD staging tối thiểu (`deploy-staging.yml`, manual). Docker Compose stack (`docker compose up`). Script backup/restore Postgres + MinIO (`scripts/backup.sh`, `scripts/restore.sh`) + runbook trong `07-infra/observability-and-backup.md`.
- ✅ ADR-0001…0010 (đã có file đầy đủ trong `01-architecture/adr/`): SPA-in-Laravel, Postgres+partition, modular monolith, connector/registry, VN-only, AntD 5, webhook+polling, master SKU, PK strategy, RBAC tự viết.
- ⏳ **Việc ngoài-code (không thuộc repo):** nộp hồ sơ Shopee + Lazada Open Platform, đăng ký app TikTok Shop Partner · bật branch protection cho `main` (require CI + 1 review) · cấu hình `SENTRY_LARAVEL_DSN` + alert rules + giám sát queue-depth ở môi trường thật · dựng host staging + secrets cho `deploy-staging.yml` · bật WAL/PITR Postgres + versioning/replicate MinIO + đẩy backup off-site + test khôi phục định kỳ.
- ⏳ **Còn lại trong code (nhỏ, không chặn Exit criteria):** contract-test job (thêm khi có connector sàn đầu tiên — Phase 1) · Prettier config FE · trang `/forgot-password` + `/onboarding` (sitemap FE) · backfill seeder/factory cho các bảng module khi schema ra đời.

## Phase 1 — TikTok Shop: Đồng bộ đơn  ◑ (code xong; còn kiểm thử với sandbox thật)
**Việc:** OAuth connect/disconnect gian hàng TikTok · HTTP client PHP cho TikTok (auth + ký HMAC, refresh token, orders, order detail) versioned · webhook receiver `/webhook/tiktok` + verify chữ ký + `ProcessWebhookEvent` job · polling `SyncOrdersForShop` (5–15') + backfill 90 ngày · trạng thái chuẩn + mapping TikTok + `order_status_history` · màn Đơn hàng (list/filter/detail/đổi trạng thái/tag/note) + Dashboard cơ bản · auto refresh token + cảnh báo re-connect.
**Exit criteria:** kết nối shop TikTok thật → đơn mới tự xuất hiện trong vòng vài phút (qua webhook hoặc polling), xem/lọc/đổi trạng thái được; mất webhook vẫn không mất đơn nhờ polling.

**Trạng thái (2026-05-12 · spec [`docs/specs/0001-tiktok-order-sync.md`](../specs/0001-tiktok-order-sync.md)):**
- ✅ Module **Channels** (`channel_accounts`, `oauth_states`, `webhook_events`, `sync_runs`) + module **Orders** (`orders`, `order_items`, `order_status_history`) — migrations, models, `OrderUpsertService` idempotent, `OrderStateMachine`, domain events.
- ✅ **TikTok connector** (`app/Integrations/Channels/TikTok/`): `TikTokConnector` (orders+auth+webhook; listing/fulfillment/finance khai `capability=false` cho Phase sau) · `TikTokClient` (ký HMAC-SHA256, version `202309`, refresh token, throttle per shop) · `TikTokSigner` · `TikTokStatusMap` · `TikTokMappers` (raw→DTO) · `TikTokWebhookVerifier`. Đăng ký `ChannelRegistry::register('tiktok', …)` + `INTEGRATIONS_CHANNELS=manual,tiktok` + block `config/integrations.tiktok` (sandbox vs prod = config; app key/secret từ `.env`).
- ✅ OAuth connect/disconnect/resync (`/api/v1/channel-accounts*` + callback `/oauth/tiktok/callback` + `WebhookController` cho `/webhook/tiktok`); webhook verify→ghi→`ProcessWebhookEvent` (re-fetch detail rồi upsert); polling `SyncOrdersForShop` (scheduled ~10') + `BackfillOrders`; auto refresh token (`RefreshChannelToken` + lệnh `channels:refresh-expiring-tokens` mỗi 30') → fail thì `channel_account.status=expired` + event `ChannelAccountNeedsReconnect`.
- ✅ API: `GET /channel-accounts`, `POST /channel-accounts/{provider}/connect`, `DELETE /channel-accounts/{id}`, `POST /channel-accounts/{id}/resync`, `GET /orders`(filter/sort/pagination), `GET /orders/{id}`, `GET /orders/stats`, `POST /orders/{id}/tags`, `PATCH /orders/{id}/note`, `GET /dashboard/summary`.
- ✅ FE phong cách BigSeller: layout sidebar nhóm + header (workspace switcher / chuông / user) · Dashboard (stat cards + to-do) · **Đơn hàng** (status tabs có badge số lượng, filter bar, bảng dày, phân trang) · **Chi tiết đơn** (timeline trạng thái, người nhận, kiện hàng, tags/note) · **Gian hàng** (thẻ gian hàng, kết nối TikTok → redirect OAuth, resync/disconnect, xử lý callback) · **Nhật ký đồng bộ** (2 tab: *Lần đồng bộ* = `sync_runs` với loại/trạng thái/kết quả, *Webhook* = `webhook_events` với loại sự kiện/chữ ký/trạng thái; lọc theo gian hàng, nút **chạy lại** cho owner/admin) · component chung `StatusTag`/`MoneyText`/`DateText`/`ChannelBadge`/`PageHeader`.
- ✅ API nhật ký đồng bộ: `GET /sync-runs`, `POST /sync-runs/{id}/redrive`, `GET /webhook-events` (không lộ payload thô), `POST /webhook-events/{id}/redrive` (`channels.view` để xem, `channels.manage` để re-drive).
- ✅ **Webhook fast-path** (2026-05-15): `parseWebhook` trích `data.order_status` → cột `webhook_events.order_raw_status`; `ProcessWebhookEvent` áp trạng thái này ngay vào đơn đã có (`OrderUpsertService::applyStatusFromWebhook`, không bump `source_updated_at`) trước/khi `fetchOrderDetail` lỗi ⇒ webhook cập nhật trạng thái đơn dù API re-fetch tạm hỏng (sandbox). + nút "Đồng bộ đơn" trong trang Đơn hàng (`POST /orders/sync` — dispatch `SyncOrdersForShop` mọi shop active) + alias tên shop (`channel_accounts.display_name`, `PATCH /channel-accounts/{id}`, UI rename) vì shop có thể trùng tên.
- ✅ Test: contract TikTok (fixtures, `Http::fake`), feature webhook→upsert (idempotent) + webhook fast-path (re-fetch lỗi → áp trạng thái từ payload) + polling→upsert + refresh token + orders API + tenant isolation + connect flow + nhật ký đồng bộ; unit `TikTokSigner`/`OrderStateMachine`/`StandardOrderStatus`.
- ⏳ **Còn lại cho Exit criteria đầy đủ:** kết nối shop TikTok **sandbox thật** (cần `APP_URL` HTTPS công khai để TikTok redirect callback — dùng ngrok cho dev; app key/secret đã có trong `.env`) và xác nhận đơn mới tự về; rà soát mapping webhook event-type + order status với tài liệu Partner API thật; hoàn thiện rate-limit Redis throttle per shop; chuyển `orders`/`order_items`/`order_status_history` sang partition theo tháng khi volume cần.

## Phase 2 — Đơn thủ công + SKU + Tồn kho lõi + Sổ khách hàng  ◑ (backend đủ exit criteria; còn FE đầy đủ + sửa line-item đơn manual + Vitest)
**Việc:** sản phẩm/SKU master · kho (warehouses) + `inventory_levels` + `inventory_movements` (sổ cái) · tạo đơn thủ công (reserve tồn) · màn Liên kết SKU (manual + auto-match `seller_sku == sku_code`, hỗ trợ combo 1→N) cho listing TikTok · `PushStockToChannel` (debounce + distributed lock + safety stock) đẩy tồn lên TikTok · cảnh báo hết hàng/âm kho · **Sổ khách hàng & cờ rủi ro** (module `Customers` mới — SPEC [0002](../specs/0002-customer-registry-and-buyer-reputation.md)): match đơn theo SĐT chuẩn hoá → hồ sơ khách + lifetime stats + reputation badge + manual notes; card "Khách hàng" ở Order detail giúp NV soi lịch sử (huỷ/hoàn/giao thất bại) **trước khi xác nhận đơn**; auto-note khi vượt ngưỡng; block khách (rules engine Phase 6 sẽ tự xử lý).
**Exit criteria:** bán TikTok + tạo đơn tay đều trừ chung 1 kho; thay đổi tồn → tự đẩy lên listing TikTok liên kết; mọi thay đổi tồn có dòng trong sổ cái; **đơn mới về tự khớp vào hồ sơ khách (nếu có SĐT) và hiển thị lịch sử + reputation ở chi tiết đơn**.

**Trạng thái (2026-05-13 · spec [`docs/specs/0002-customer-registry-and-buyer-reputation.md`](../specs/0002-customer-registry-and-buyer-reputation.md)):**
- ✅ Module **Customers** (mới): `customers` + `customer_notes` migrations + thêm cột `orders.customer_id` (soft ref) · models `Customer`/`CustomerNote` · `CustomersServiceProvider` (bind `CustomerProfileContract` → `CustomerProfileResolver` + `CustomerProfileDTO`).
- ✅ `CustomerPhoneNormalizer` (chuẩn hoá VN `+84/84 → 0`, loại SĐT mask, canonical quốc tế) + `sha256` hash · `ReputationCalculator` (heuristic v1, config `config/customers.php`).
- ✅ Listener `LinkOrderToCustomer` (listen `OrderUpserted`, queue `customers`) → khớp/tạo khách theo `phone_hash`, set `orders.customer_id`, recompute `lifetime_stats` + reputation + auto-notes (dedupe theo ngưỡng) — idempotent (đọc thẳng từ `orders`).
- ✅ Block/unblock/merge (chuyển `orders.customer_id` + `customer_notes`, soft-delete bên bị gộp), tags, notes (manual + xoá note của mình) · domain events `CustomerLinked`/`CustomerReputationChanged`/`CustomerBlocked`/`CustomerUnblocked`/`CustomersMerged`.
- ✅ Ẩn danh hoá: event `DataDeletionRequested` (Channels phát từ `ProcessWebhookEvent`) + `ChannelAccountRevoked` → job `AnonymizeCustomersForShop` (ngay với data_deletion, trễ `customers.anonymize_after_days`≈90 ngày với disconnect) — giữ `phone_hash`/`lifetime_stats`, clear `phone`/`name`/`email`/`addresses_meta`; khách còn đơn ở shop khác thì giữ.
- ✅ Commands: `customers:backfill` (one-shot, khôi phục từ `orders`), `customers:recompute-stale` (scheduled hằng giờ — safety net) · queue `customers` thêm vào supervisor low-priority của Horizon.
- ✅ API: `GET /customers`(filter/sort/search SĐT→hash), `GET /customers/{id}`(+notes), `GET /customers/{id}/orders`, `POST /customers/{id}/notes`, `DELETE …/notes/{noteId}`, `POST …/block` / `…/unblock` / `…/tags`, `POST /customers/merge` · `OrderResource` thêm field `customer` qua `CustomerProfileContract`.
- ✅ FE: trang **Khách hàng** (`/customers` list: filter reputation tab + search + sort, `/customers/:id`: thông tin + ghi chú + lịch sử đơn + block) · `<ReputationBadge>` · `<CustomerSummaryCard>` (card "Khách hàng" ở `OrderDetailPage`) · mục sidebar "Khách hàng".
- ✅ RBAC: `customers.view` (mọi role), `customers.note`/`customers.view_phone` (owner/admin/staff_order), `customers.block`/`customers.merge` (owner/admin) · test: unit `CustomerPhoneNormalizer`/`ReputationCalculator`, feature linking (idempotent, matching đa-format, tenant isolation, vip), API (list/filter/search/notes/block/tags/merge/orders + customer card + phân quyền), anonymize.
- ✅ **SKU / Tồn kho lõi / Đơn thủ công / Ghép SKU / Đẩy tồn** (spec [`docs/specs/0003-...md`](../specs/0003-products-skus-inventory-manual-orders.md)): module **Products** (`products`, `channel_listings`) + module **Inventory** (`skus` master · `warehouses` + `Warehouse::defaultFor()` tự tạo · `inventory_levels` on_hand/reserved/safety/available_cached/is_negative · `inventory_movements` sổ cái bất biến + balance_after · `sku_mappings` single/bundle) · `InventoryLedgerService` (adjust/receipt/reserve/release/ship/returnIn — transaction + `lockForUpdate`, idempotent per `(order_item,sku,type)`, chống oversell ⇒ `available`=0 + cờ âm kho, phát `InventoryChanged`) · listener `ApplyOrderInventoryEffects` (listen `OrderUpserted`: resolve `order_items.sku_id` qua mapping/auto-match `seller_sku==sku_code`, áp tồn theo vòng đời đơn — *thay no-op stub Phase 1*; set/clear `has_issue='SKU chưa ghép'`) · `ManualOrderService` + `POST/PATCH /orders` + `POST /orders/{id}/cancel` (đơn `source=manual`, reserve ngay, fire `OrderUpserted` ⇒ link khách SPEC 0002) · `SkuMappingService` + API ghép/auto-match · **PushStockToChannel**: `InventoryChanged` → `PushStockForSku` (debounce: ShouldBeUnique per sku + delay ~10s) → `PushStockToListing` (queue `inventory-push`) → `connector.updateStock` · TikTok bật `listings.updateStock=true` + implement `updateStock` (signed call, `$context` carry product_id; *shape cần xác nhận với Partner API*) · `ChannelConnector::updateStock` thêm tham số `$context` · RBAC `products.manage`/`inventory.map` · API: `products`, `skus`, `warehouses`, `inventory/levels|adjust|movements`, `channel-listings`, `sku-mappings` (+ phân quyền + tenant isolation). Test: unit `SkuCodeNormalizer` · feature `InventoryLedger`/`OrderInventoryEffects`/`ManualOrder`/`InventoryApi`/`PushStock` (+ TikTok updateStock qua `Http::fake`). (143 test xanh.)
- ✅ **TikTok `fetchListings`** (2026-05-15): capability `listings.fetch=true`; `/product/202309/products/search` → flatten product→sku → `ChannelListingDTO[]` (`TikTokMappers::listings`); `ChannelConnector::fetchListings()` (+ `updateStock($context)`). Job `Channels\Jobs\FetchChannelListings` (queue `listings`, unique/shop) → upsert `channel_listings` `(channel_account_id,external_sku_id)` → `autoMatchUnmapped`. API `POST /channel-accounts/{id}/resync-listings` + `POST /channel-listings/sync`; scheduler hằng ngày; command `inventory:resync-order-skus`; FE nút "Đồng bộ listing từ sàn" ở tab Liên kết SKU. Test contract `FetchChannelListingsTest` (`Http::fake` product/search → upsert + auto-match + idempotent). → Phase 2 đạt đủ Exit criteria (147 test xanh).
- ✅ **Tồn kho thủ công hàng loạt + Liên kết SKU nhanh từ đơn** (2026-05-15 · spec [`docs/specs/0004-...md`](../specs/0004-bulk-stock-and-sku-linking.md)): `POST /inventory/bulk-adjust` (phiếu nhập/xuất hàng loạt — mỗi dòng 1 movement `ref_type='manual_bulk'`), `POST /inventory/push-stock` (đẩy tồn hàng loạt theo bộ chọn), `GET /orders/unmapped-skus` (gộp SKU sàn distinct + gợi ý master SKU theo code), `POST /orders/link-skus` (firstOrCreate `channel_listing` + `sku_mapping` single×1 + re-fire `OrderUpserted` cho mọi đơn còn chưa ghép ⇒ resolve sku_id/reserve/clear has_issue/đẩy tồn) · `stats.unmapped` · FE: đơn chưa ghép có tag đỏ "Chưa liên kết SKU — Liên kết" + `Alert` ở Order detail + banner ở list + `rowSelection` + nút "Liên kết SKU (n)" + `<LinkSkusModal>` (tự tải lại tồn/đơn); tab SKU có "Phiếu nhập/xuất hàng loạt" + "Đẩy tồn lên sàn (n)" · test `BulkInventoryTest`(5) + `LinkOrderSkusTest`(4). (156 test xanh.)
- ☐ **Chưa làm (đẩy sang Phase sau / backlog):** sửa line-item của đơn manual sau khi tạo (hiện huỷ + tạo lại) · cảnh báo realtime hết hàng/âm kho (Phase 6 — Notifications; hiện chỉ flag UI) · FE đầy đủ cho Sản phẩm/Tồn kho + Vitest. WMS đầy đủ — "phiếu" có header/duyệt, nhập/xuất/điều chuyển/kiểm kê/giá vốn FIFO — Phase 5.

## Phase 3 — Giao hàng & in ấn (TikTok)  ◑ (lõi xong — luồng ĐVVC riêng + Manual + GHN; còn luồng-logistics-của-sàn / GHTK+JT / template tuỳ biến / lưu-in-lại-90-ngày)
**Việc:** luồng "sắp xếp vận chuyển" TikTok → lấy tracking + label PDF → lưu MinIO · **in vận đơn hàng loạt** (ghép PDF, sắp theo ĐVVC) · **picking/packing list** render bằng Gotenberg + **template tùy biến** · **quét mã đóng gói** → xác nhận đóng/bàn giao → trừ tồn → trạng thái shipped · kết nối ĐVVC riêng đợt 1: **GHN + GHTK + J&T** (quote/createShipment/getLabel/track/cancel) cho đơn manual & đơn tự xử lý · lô lấy hàng (pickup batch).
**Exit criteria:** từ list đơn → tạo vận đơn hàng loạt → in tem 1 file → quét từng kiện để xác nhận đóng gói → bàn giao ĐVVC → trạng thái & tồn cập nhật đúng.

**Trạng thái (2026-05-17 · spec [`docs/specs/0006-fulfillment-shipments-and-printing.md`](../specs/0006-fulfillment-shipments-and-printing.md)):**
- ✅ Module **Fulfillment** (lần đầu có code): `carrier_accounts` (creds ĐVVC mã hoá `encrypted:array`), `shipments` + `shipment_events`, `print_jobs` (bản tối giản) — migrations, models (`CarrierAccount`/`Shipment`/`ShipmentEvent`/`PrintJob`), events `ShipmentCreated`/`PrintJobCompleted`.
- ✅ **Carrier layer**: `AbstractCarrierConnector` + `ManualCarrierConnector` (luôn có sẵn, không cần creds — người bán tự nhập/quản tracking, không label) + `GhnConnector` (+ `GhnClient`, `GhnStatusMap` — gọi API GHN thật: create-order / printA6 / order-detail / cancel) đăng ký qua `CarrierRegistry` (`manual` luôn nạp; phần còn lại theo `INTEGRATIONS_CARRIERS`). GHTK/J&T = follow-up (đã sẵn khung).
- ✅ **ShipmentService**: tạo vận đơn cho đơn (đơn lẻ + hàng loạt, 1 đơn = 1 shipment chưa huỷ, lỗi từng đơn không chặn batch) → gọi `connector.createShipment` → tải label PDF (`connector.getLabel`) lưu lên kho media (R2) → đơn `processing→ready_to_ship` · bàn giao/quét đóng gói (`picked_up`) → đơn `→shipped` · huỷ vận đơn (`connector.cancel`) → đơn về `processing` nếu chưa shipped · track (`connector.getTracking`) → dedupe `shipment_events` + map trạng thái → đồng bộ đơn. Đồng bộ đơn đi qua `OrderStatusSync` ⇒ phát `OrderUpserted` ⇒ ledger trừ/hoàn tồn tự động (SPEC 0003) + `order_status_history`.
- ✅ **In ấn**: `PrintService` + job `RenderPrintJob` (queue `labels`, rule 3) — `label` = ghép các label PDF bằng **Gotenberg** (`/forms/pdfengines/merge`); `picking` = phiếu soạn hàng gộp theo SKU (HTML template dựng sẵn → Gotenberg → PDF); `packing` = mỗi đơn 1 phiếu. `GotenbergClient` (Support). FE poll `print_jobs` đến khi `done` rồi mở file. (Template tuỳ biến `print_templates` + lưu-in-lại-90-ngày `order_print_documents`/`PrunePrintDocuments` = follow-up — domain doc §8.)
- ✅ **Quét đóng gói**: `POST /scan-pack {code}` resolve theo mã vận đơn hoặc mã đơn (trong tenant) → đánh dấu shipment `picked_up`, đơn `shipped`, trừ tồn (`order_ship`, idempotent); quét trùng ⇒ `409`, không thấy ⇒ `404`. Job scheduled `SyncShipmentTracking` (~30').
- ✅ API: `carrier-accounts` CRUD + `GET /carriers`; `GET /fulfillment/ready` (đơn cần giao); `POST /orders/{id}/ship` + `POST /shipments/bulk-create` + `GET /shipments` + `GET /shipments/{id}` + `POST /shipments/{id}/track|cancel` + `GET /shipments/{id}/label` + `POST /shipments/handover` + `POST /scan-pack`; `POST/GET /print-jobs` + `GET /print-jobs/{id}/download`. `OrderResource` thêm `shipment`. RBAC: `fulfillment.view/ship/scan/print` (đã có trong `Role`) + `fulfillment.carriers` (owner/admin).
- ✅ FE: trang **Giao hàng** `/fulfillment` (thay ComingSoon) — 3 tab *Cần giao* (chọn nhiều đơn → tạo vận đơn / picking / packing) · *Vận đơn* (danh sách shipments + track/huỷ/in tem/bàn giao + modal timeline) · *Quét đóng gói* (ô quét auto-focus + log phiên) + thanh tiến trình print job · **Cài đặt → ĐVVC** `/settings/carriers` (CRUD carrier account, form theo carrier) · card "Vận đơn" ở chi tiết đơn · cột vận đơn ở danh sách đơn (qua `OrderResource.shipment`). `lib/fulfillment.tsx` hooks.
- ✅ Test: `Fulfillment\FulfillmentTest` — carrier-account CRUD + RBAC + không lộ creds + tenant isolation; luồng xử lý đơn (tạo vận đơn → scan-pack đóng gói → scan-handover bàn giao + `order_ship` movement → quét lại 409 / mã lạ 404 → huỷ → về processing) + bulk pack/handover; bulk-create báo lỗi từng đơn; GHN connector qua `Http::fake` (create/label/track) + label lưu kho media + đơn delivered sau track; print job label bundle (`Storage::fake`) + đếm `print_count` (in lại → tăng) + chặn ghép tem 2 ĐVVC (422) + picking list gộp theo SKU + 422 khi thiếu input + RBAC. (180 test xanh.)
- ✅ **Màn "Xử lý đơn hàng"** (2026-05-18 · spec [`docs/specs/0009-order-processing-screen.md`](../specs/0009-order-processing-screen.md), tài liệu nền [`docs/04-channels/order-processing.md`](../04-channels/order-processing.md)): trạng thái vận đơn mới **`packed`** (giữa `created` và `picked_up`); cột `shipments.print_count`/`last_printed_at`/`packed_at`. `markPacked()` (đóng gói — không trừ tồn) tách khỏi `handover()` (bàn giao — đơn `shipped`, trừ tồn). API `GET /fulfillment/processing?stage=prepare|pack|handover` (+`/counts`) lọc `source`/`carrier`/`customer`/`product`; `POST /shipments/pack` (bulk đóng gói); `POST /scan-pack` (= đóng gói) & `POST /scan-handover` (= bàn giao — app quét đơn gọi cái này). `PrintService` đếm `print_count` mỗi lần in tem + chặn in tem khi bộ chọn lẫn nhiều nền tảng/ĐVVC (422); `RenderPrintJob` chạy đúng tenant (fix scope khi dùng queue thật). FE: **xử lý đơn nằm ngay trong trang Đơn hàng** (`/orders` — BigSeller-style, không tách trang; route cũ `/fulfillment` redirect về `/orders?tab=prepare`, bỏ mục sidebar riêng) — **3 bước = 3 tab nằm cùng dải tab với các tab trạng thái đơn** (không lồng sub-tab): `📦 Cần chuẩn bị` · `🖨 Chờ đóng gói` · `🚚 Chờ bàn giao ĐVVC` + `🏷 Vận đơn` (Badge số lượng); chọn tab-bước ⇒ chips nền tảng (TikTok/Shopee/Lazada/Đơn tay, có màu) + 2 ô lọc (khách hàng / sản phẩm) + bảng đơn của bước (`StageBoard`); quét = nút mở modal (Segmented Đóng gói/Bàn giao). Badge "🖨 N×" mỗi dòng; nút "In tem" disable+tooltip khi lẫn nền tảng/ĐVVC, popup xác nhận khi in lại. Component dùng chung: `components/OrderProcessing.tsx` (`StageBoard`/`ScanTab`/`ShipmentsTab`/`PrintJobBar`/`PlatformChips`).
- ⏳ **Còn lại (follow-up Phase 3 / Phase sau):** luồng A — TikTok "arrange shipment" + lấy label/invoice của sàn (cần xác nhận shape Partner API) · connector **GHTK, J&T Express** (+ đợt 2: ViettelPost, NinjaVan, SPX, VNPost, Best, Ahamove/Grab) · resolve địa chỉ free-text → mã quận/phường GHN · **template in tuỳ biến** + **lưu & in lại phiếu 90 ngày** (`print_templates`, `order_print_documents`, `expires_at/purged_at`, `PrunePrintDocuments` — spec riêng) · **pickup batch** (lô bàn giao có phiếu ký nhận) · carrier webhook · tách nhiều kiện/đơn (`order.is_split`) · đối soát phí ship thực tế (Phase 6) · audit log cho thao tác in/tạo-huỷ vận đơn nếu cơ chế audit chưa wired.

## Phase 4 — Shopee + Lazada  ◑ (Lazada lõi xong; Shopee + RTS/AWB của 2 sàn còn lại)  *(bắt đầu sau khi có API)*
**Việc:** connector + OAuth + client + status map + sync đơn (push+pull) + listing + ghép SKU + push tồn + luồng in/label cho **Shopee** và **Lazada** · xử lý khác biệt (đơn nhiều kiện, COD, cấu trúc phí, document API).
**Exit criteria:** 3 sàn + đơn tay chạy chung một luồng; tồn đồng bộ chéo sàn; in tem cho cả 3 sàn.

**Trạng thái (2026-05-17 · spec [`docs/specs/0008-lazada-channel.md`](../specs/0008-lazada-channel.md)):**
- ✅ **Lazada connector** (`app/Integrations/Channels/Lazada/`): `LazadaConnector` (implements `ChannelConnector`) + `LazadaClient` (ký HMAC-SHA256 `sha256` qua `LazadaSigner`, gateway `api.lazada.vn`, auth host `auth.lazada.com`, throttle per shop, envelope `{code,type,message,data}`) + `LazadaStatusMap` (+ `collapse()` cho status item-level) + `LazadaMappers` (raw→DTO chuẩn, tiền→VND đồng) + `LazadaWebhookVerifier` (chữ ký HMAC raw body) + `LazadaApiException`. Capabilities: `orders.fetch/webhook`, `listings.fetch`, `listings.updateStock` = true; `shipping.*` (RTS/AWB), `finance.*`, `returns.*`, `listings.publish/updatePrice` = false (Phase sau). Đăng ký `ChannelRegistry` qua `INTEGRATIONS_CHANNELS=…,lazada`; block `config/integrations.lazada` (hosts, endpoint paths config-able, `status_map`, `webhook_message_types`); `.env.example` + throttle.
- ✅ Tái dùng nguyên hạ tầng Phase 1–3 (provider-agnostic): OAuth connect/disconnect/callback (`/oauth/lazada/callback`), `SyncOrdersForShop` poll + backfill, `RefreshChannelToken`, webhook `/webhook/lazada` → `ProcessWebhookEvent` (fast-path `orderRawStatus` + re-fetch detail), `OrderUpsertService`, listener tồn (reserve/ship) + link khách, `FetchChannelListings` + auto-match SKU, `PushStockForSku`/`PushStockToListing` → `updateStock`. Lazada riêng: `fetchOrders` offset paging + gộp items qua `/orders/items/get?order_ids=[…]`; `fetchOrderDetail` = `/order/get` + `/order/items/get`; `fetchListings` = `/products/get` flatten SKU; `updateStock` = `/product/price_quantity/update` (`payload` JSON, hoặc XML nếu `LAZADA_UPDATE_STOCK_FORMAT=xml`). Lazada không có API subscribe webhook per-shop ⇒ đăng ký ở console; `external_product_id` ở order-item dùng `shop_sku` làm thay.
- ✅ FE không cần code mới — trang Gian hàng/Đơn hàng/Liên kết SKU đã generic; `CHANNEL_META.lazada` đã có; nút "Kết nối Lazada" tự hiện khi connector được nạp (`connectable_providers` đọc từ `ChannelRegistry`).
- ✅ Test: `Channels\LazadaConnectorContractTest` (11 ca, `Http::fake`) — token exchange/refresh, shop info, fetchOrders (gộp items/tiền VND/COD/status/package dedup), fetchOrderDetail, mapStatus (collapse item-level + status lạ không ném), fetchListings, updateStock (assert payload), webhook verify (đúng/sai/thiếu) + parse, `arrangeShipment` ném `UnsupportedOperation`, signer deterministic + vector cố định, registry resolve. (180 test xanh.)
- ⏳ **Còn lại cho Exit criteria:** **Shopee connector** (cấu trúc tương tự — spec riêng) · **RTS / đóng gói / in AWB+invoice+packing list của Lazada** (`/logistic/...`, `/order/document/get` → base64 PDF → kho media) nối vào module Fulfillment như "luồng A — logistics của sàn" (cũng cho TikTok) · kết nối shop Lazada **sandbox thật** (cần `APP_URL` HTTPS công khai) → xác nhận đơn về & đối chiếu `webhook_message_types`/`status_map` với dữ liệu thật · đăng/sửa listing đa sàn (Phase 5) · đối soát Lazada (`/finance/...`, Phase 6).

## Phase 5 — WMS đầy đủ + Đăng bán đa sàn  ◑ (WMS phiếu lõi xong; còn FIFO / đăng-bán-đa-sàn / ĐVVC đợt 2)
**Việc:** nhập/xuất/điều chuyển kho + kiểm kê (có phiếu, chênh lệch) + giá vốn FIFO/bình quân · đăng sản phẩm lên nhiều sàn từ 1 SP gốc + sao chép listing + sửa hàng loạt + đồng bộ category/attribute sàn · ĐVVC đợt 2 (ViettelPost, NinjaVan, SPX, VNPost, Best, Ahamove/Grab).
**Exit criteria:** quản lý kho khép kín (nhập→bán→kiểm kê→báo cáo tồn) + đăng/sửa listing đa sàn từ một nơi.

**Trạng thái (2026-05-19 · spec [`docs/specs/0010-wms-documents.md`](../specs/0010-wms-documents.md)):**
- ✅ **WMS — phiếu có header + duyệt** (như đã hứa từ SPEC-0004): 6 bảng `goods_receipts`/`stock_transfers`/`stocktakes` (+ `_items`) — phiếu `draft → confirmed → cancelled`; `WarehouseDocumentController` (route theo `{type}` ∈ `goods-receipts|stock-transfers|stocktakes`: index/show/store/confirm/cancel) + `WarehouseDocumentService` (confirm áp vào sổ cái `InventoryLedgerService`: nhập kho → `receipt()` + **giá vốn bình quân theo kho** `updateAverageCost()`; chuyển kho → `transferOut()` + `transferIn()`; kiểm kê → re-snapshot `system_qty` + `stocktakeAdjust(diff)`) + 3 method ledger mới (`transferOut`/`transferIn`/`stocktakeAdjust`) + `onHand()`. Phiếu confirmed bất biến (sửa = phiếu mới) ⇒ sổ cái là dấu vết kiểm toán; confirm phát `InventoryChanged` ⇒ đẩy tồn lên sàn. RBAC: `inventory.adjust`/`transfer`/`stocktake` (đã có trong `Role` — StaffWarehouse có cả 3). FE: tab "Phiếu kho" ở trang Tồn kho (`/inventory?tab=docs` — `components/WarehouseDocsTab.tsx`): Segmented chọn loại + bảng phiếu + nút Xác nhận/Huỷ + modal tạo phiếu (kho = `Radio.Group` button — không Select; dòng = `Form.List` + `<SkuPickerField>` + `InputNumber`) + modal xem chi tiết; `lib/inventory` `useWarehouseDocs/...`. Test `WarehouseDocumentsTest` (5 ca: nhập kho confirm ⇒ +tồn + giá vốn bình quân + movement / immutable confirmed (422); huỷ draft; chuyển kho ⇒ from-/to+ + transfer_out/in / from==to (422); kiểm kê ⇒ system_qty snapshot + diff + stocktake_adjust; RBAC + tenant isolation). (186 test xanh.)
- ✅ **Trung tâm Cài đặt — vỏ + Hồ sơ + Thông tin gian hàng** (2026-05-19 · spec [`docs/specs/0011-settings-shell.md`](../specs/0011-settings-shell.md) — slice đầu của umbrella SPEC-0007): `components/SettingsLayout.tsx` (PageHeader + menu trái phân nhóm Tài khoản / Nhân sự & phân quyền / Kết nối / Vận hành — nhóm chưa làm = "Sắp có"); routes lồng `/settings/{profile,workspace,members,carriers}`, `/settings` → `profile`; sidebar "Cài đặt" → `/settings`. `SettingsProfilePage` (sửa tên/email + đổi mật khẩu — `Input`/`Input.Password`, **không Select**), `SettingsWorkspacePage` (sửa tên gian hàng + slug — owner/admin; vai trò khác = chỉ-xem). BE: `PATCH /auth/profile` (đổi email/password cần `current_password`), `PATCH /tenant` (name/slug/settings — `tenant.settings`/owner+admin, settings merge, ghi `audit_logs`). FE lib: `useUpdateProfile`/`useUpdateTenant`.
- ✅ **Lợi nhuận ước tính sau phí sàn + cài đặt giá vốn + in hoá đơn** (2026-05-12 · spec [`docs/specs/0012-order-profit-and-invoice-print.md`](../specs/0012-order-profit-and-invoice-print.md) — slice "cài đặt đơn hàng" của SPEC-0007 + mở rộng SPEC-0003/0006/0010): `skus.cost_method` (`average`|`latest`) + `skus.last_receipt_cost`; xác nhận phiếu nhập kho cập nhật giá vốn bình quân **và** `last_receipt_cost` (`InventoryLedgerService::recordReceiptCost`); `tenant.settings.platform_fee_pct` (% phí sàn theo nền tảng) cấu hình ở trang `/settings/orders` (`SettingsOrdersPage` — `InputNumber`, **không Select**); `OrderProfitService` tính `profit{cogs,platform_fee,shipping_fee,estimated_profit,platform_fee_pct,cost_complete}` trên `OrderResource` (list: 1 query batched); FE hiển thị cột "Lợi nhuận ƯT" ở danh sách đơn + khối lợi nhuận ở chi tiết đơn (cảnh báo ⚠ khi thiếu giá vốn SKU); `Radio.Group` "Cách tính giá vốn" ở form SKU; `print_jobs.type='invoice'` + `PrintTemplates::invoice` (hoá đơn một trang/đơn) + nút "In hoá đơn" ở chi tiết đơn. **Sửa lỗi:** `POST /orders/link-skus` re-resolve **đồng bộ** mọi đơn còn dòng chưa ghép (trước đây re-fire `OrderUpserted` async ⇒ đơn cùng SKU sàn không tự ghép tới khi queue chạy). Test: `OrderApiTest` (profit list/detail + invoice print job), `InventoryApiTest` (`cost_method`/`effective_cost`). **UI xử lý đơn (rà SPEC-0009):** bỏ 3 tab-bước riêng `Cần chuẩn bị / Chờ đóng gói / Chờ bàn giao ĐVVC` — các thao tác đó (Tạo vận đơn · In tem · Đóng gói · Bàn giao ĐVVC · In hoá đơn) giờ là nút trong cột "Thao tác" ngay trên các tab trạng thái đơn có sẵn; tab "Vận đơn" + nút "Quét đơn" giữ nguyên; luồng nghiệp vụ không đổi.
- ✅ **Sửa luồng xử lý đơn + chặn in phiếu khi hết hàng** (2026-05-12 · spec [`docs/specs/0013-order-fulfillment-flow-and-out-of-stock.md`](../specs/0013-order-fulfillment-flow-and-out-of-stock.md) — rà SPEC-0009): map "đơn sàn đã arrange/in phiếu" (TikTok `AWAITING_COLLECTION`, Lazada `packed`/`ready_to_ship`, Shopee `PROCESSED`) → `processing` (không phải `ready_to_ship`); `ready_to_ship` chỉ đạt được bằng thao tác nội bộ (`markPacked` khi "đã gói & quét"). `createForOrder` (="Chuẩn bị hàng/lấy phiếu") nay đẩy đơn `pending → processing` và **chặn nếu đơn có SKU âm tồn** (`∑on_hand − ∑reserved < 0` ⇒ `422`) — in phiếu mà không có hàng ⇒ ĐVVC bị phạt; `InventoryLedgerService::netStockForSku()`. `OrderResource.out_of_stock` + filter `GET /orders?out_of_stock=1` + `stats.out_of_stock` + tab "⚠️ Hết hàng". `print_jobs.type='delivery'` + `PrintTemplates::deliverySlip` (phiếu giao hàng tự tạo cho bước "Chuẩn bị hàng"; kéo tem/AWB thật của sàn = "luồng A" follow-up). 3 tab "công việc" (`Chờ xử lý`/`Đang xử lý`/`Chờ bàn giao`) lọc theo **bước/vận đơn** (`GET /orders?stage=prepare|pack|handover` + `stats.by_stage` — `prepare`=đơn trước-giao **chưa có vận đơn** = "chưa chuẩn bị hàng", **mọi nguồn** không chỉ manual; `pack`=vận đơn `pending`/`created`; `handover`=vận đơn `packed`), không theo trạng thái đơn nữa. FE cột "Thao tác" hiện nút theo **vận đơn**: chưa có vận đơn⇒"Chuẩn bị hàng"/hết hàng · vận đơn pending/created⇒"In phiếu giao hàng"/"Đã gói & sẵn sàng bàn giao" · vận đơn packed⇒"Bàn giao ĐVVC"; tab "Chờ xử lý" có tick chọn + nút **"Chuẩn bị hàng (N)"** hàng loạt. "Chuẩn bị hàng" gọi `ChannelConnector::arrangeShipment` (cập nhật trạng thái "đã in đơn" lên sàn — TikTok wired sau cờ `INTEGRATIONS_TIKTOK_FULFILLMENT`, Shopee/Lazada follow-up). UI dùng font icon (`@ant-design/icons`), không emoji (rule mới `06-frontend/overview.md` §4). Lệnh `php artisan media:healthcheck` kiểm cấu hình R2. Test: `FulfillmentTest` (luồng mới + out_of_stock chặn/flag/filter + delivery print + stage filter + by_stage), connector status-map tests.
- ⏳ **Còn lại Phase 5 / Phase sau:** giá vốn **FIFO** (`cost_layers`) — hiện chỉ bình quân gia quyền theo kho · phiếu **xuất kho thủ công** có header (huỷ/biếu — hiện vẫn dùng `bulk-adjust` của SPEC-0004) · sửa dòng phiếu draft sau khi tạo · duyệt nhiều cấp · phiếu liên kết PO/NCC (Phase 6 Procurement) · **đăng/sửa listing đa sàn** (đăng SP lên nhiều sàn từ 1 SP gốc, sao chép listing, sửa hàng loạt, category/attribute sàn — spec riêng khi connector listing-publish sẵn sàng; hiện connector khai `listings.publish=false`) · **ĐVVC đợt 2** (ViettelPost/NinjaVan/SPX/VNPost/Best/Ahamove — mỗi cái một `CarrierConnector` mới, không sửa core) · các spec con còn lại của Cài đặt (SPEC-0007: nhân viên username-only + vai trò chi tiết, trung tâm kết nối, mẫu in tuỳ biến, gói/billing — *cài đặt đơn hàng (% phí sàn) đã làm ở SPEC-0012*).

## Phase 6 — Tài chính + Mua hàng + Báo cáo + Billing  ◑ (6.1/6.2/6.3/6.4 xong; 6.5 Automation còn lại)
**Việc:** kéo settlement từng sàn → `settlement_lines` → đối chiếu + tính lợi nhuận theo đơn/SP/gian hàng/thời gian · NCC + bảng giá nhập + PO + nhận hàng → giá vốn · báo cáo bán hàng/lợi nhuận + export · billing SaaS (gói, hạn mức `usage_counters`, dùng thử, VNPay/MoMo) · rules engine tự động hoá · thông báo đa kênh.
**Exit criteria:** biết được lãi/lỗ từng đơn; bán được gói thuê bao; tự động hoá được các thao tác lặp.

> Vì khối lượng lớn ⇒ chia 5 sub-phase, mỗi sub-phase là 1 spec độc lập, làm cuốn chiếu.

**Phase 6.1 — Mua hàng + FIFO COGS + Báo cáo  ✅ (2026-05-22 · spec [`docs/specs/0014-procurement-and-fifo-cogs.md`](../specs/0014-procurement-and-fifo-cogs.md) + [`docs/specs/0015-business-reports.md`](../specs/0015-business-reports.md)):**
- ✅ Module **Procurement**: `suppliers` (NCC: code/name/phone/email/tax/payment_terms) + `supplier_prices` (bảng giá nhập per SKU + MOQ + `is_default`) + `purchase_orders` (PO code `PO-YYYYMM-NNNN`, status `draft→confirmed→partially_received→received|cancelled`) + `purchase_order_items` (qty_ordered/qty_received cộng dồn). Cột `po_id`/`supplier_id` thêm vào `goods_receipts`.
- ✅ `PurchaseOrderService` (create/confirm/cancel/receive — `receive()` tạo `GoodsReceipt` link PO, FE redirect tới WMS confirm; mỗi PO → nhiều `GoodsReceipt` đợt nhận; listener `LinkGoodsReceiptToPO` áp `qty_received` khi receipt confirmed → set `partially_received|received`). Idempotent.
- ✅ **FIFO Cost Layers (chuẩn kế toán)**: `cost_layers` (1 layer per receipt confirm, `qty_remaining` giảm dần, `exhausted_at` khi hết) + `order_costs` **bất biến** (1 row per `order_item`, COGS thực theo FIFO khi ship, `layers_used` jsonb). `FifoCostService::recordReceiptLayer` (hook vào `WarehouseDocumentService::confirmGoodsReceipt`) + `consumeForShip` (atomic `SELECT FOR UPDATE` theo `received_at ASC`, fallback synthetic layer từ `Sku::effectiveCost()` khi tồn FIFO không đủ) + `unconsume` khi return. Event `GoodsReceiptConfirmed` cho cross-module wiring.
- ✅ `OrderProfitService` đọc COGS thực từ `order_costs` (đơn shipped) → `cost_source: fifo`; fallback ước tính (`average|latest`) với `cost_source: estimate` khi đơn chưa ship.
- ✅ Module **Reports**: `SalesReportService` (revenue/profit/topProducts theo `day|week|month`, breakdown source/shop/warehouse, granularity portable SQLite↔Postgres qua `truncDateSql`) + `toCsv` (UTF-8 BOM cho Excel Vietnamese). REST: `GET /reports/revenue|profit|top-products|export`.
- ✅ RBAC: `procurement.view`/`procurement.manage`/`procurement.receive` + `reports.view`/`reports.export`. Test: `SupplierApiTest`, `PurchaseOrderFlowTest`, `FifoCostTest`, `ReportsApiTest`.
- ✅ FE: `/procurement/suppliers` (NCC + tab bảng giá per SKU), `/procurement/purchase-orders` (PO list + drawer "Nhận hàng" → tạo GoodsReceipt → redirect WMS confirm), `/reports` (4 tabs Tổng quan/Doanh thu/Lợi nhuận/Top SP — biểu đồ `@ant-design/charts` + filter chips thời gian/sàn/gian hàng + nút Xuất CSV). Sidebar "Mua hàng" + "Báo cáo".

**Phase 6.2 — Đối soát/Settlement (Finance Reconciliation)  ✅ (2026-05-23 · spec [`docs/specs/0016-finance-settlements-and-reconciliation.md`](../specs/0016-finance-settlements-and-reconciliation.md)):**
- ✅ DTO chuẩn: `SettlementDTO` + `SettlementLineDTO` (10 fee types: `revenue` / `commission` / `payment_fee` / `shipping_fee` / `shipping_subsidy` / `voucher_seller` / `voucher_platform` / `adjustment` / `refund` / `other`). `ChannelConnector::fetchSettlements(auth, query): Page<SettlementDTO>` thêm capability `finance.fetch`.
- ✅ **TikTok**: `TikTokConnector::fetchSettlements` (capability flag `INTEGRATIONS_TIKTOK_FINANCE` — chờ đối chiếu sandbox), `TikTokMappers::settlement()` + `mapTikTokFeeType()`. **Lazada**: `LazadaConnector::fetchSettlements` qua `/finance/transaction/details/get` (flag `INTEGRATIONS_LAZADA_FINANCE`), `LazadaMappers::settlement()` + `mapLazadaFeeType()`. Manual ⇒ `UnsupportedOperation`.
- ✅ Module **Finance**: `settlements` (header: shop, external_id, period_start/end, settled_at, totals, currency='VND') + `settlement_lines` **bất biến** (idempotent dedupe `(settlement_id, fee_type, external_order_id, external_line_id, amount)` — Lazada không có stable line ID). `SettlementService::fetchForShop|reconcile|aggregateFeesForOrders`.
- ✅ Job `FetchSettlementsForShop` (scheduled hàng ngày — gated by flag). `OrderProfitService::fetchActualFees()` đọc `settlement_lines` → set `fee_source: settlement` thay vì `estimate` ⇒ lợi nhuận đơn dùng phí THỰC từ sàn, không phải `platform_fee_pct` ước tính.
- ✅ REST: `GET /finance/settlements` (list + filter shop/period/status), `GET /finance/settlements/{id}` (detail + lines), `POST /finance/settlements/sync` (manual trigger), `POST /finance/settlements/{id}/reconcile`. RBAC `finance.view`/`finance.reconcile`.
- ✅ FE `/finance/settlements` (list theo shop/kỳ + drawer xem chi tiết lines + breakdown fee_type + cảnh báo line orphan = đơn không match). Sidebar "Tài chính → Đối soát".
- ⏳ **Còn lại trước khi bật flag production:** đối chiếu shape/`fee_type` của TikTok + Lazada với dữ liệu sandbox/production thật (Lazada có thể trả thêm `item_id`/`SKU` ở line); xác nhận currency Lazada SG/MY đổi VND nếu cross-border.

**Phase 6.3 — Đề xuất nhập hàng (Demand Planning)  ✅ (2026-05-23 · spec [`docs/specs/0017-demand-planning.md`](../specs/0017-demand-planning.md)):**
- ✅ `DemandPlanningService::compute(window_days, lead_time, cover_days, filters)` — công thức chuẩn supply-chain: `avg_daily_sold = Σ order_costs.qty / window_days` (chỉ đơn đã ship, không đếm huỷ/trả) · `available = on_hand − reserved` · `on_order = Σ (qty_ordered − qty_received)` trên PO open · `days_left = floor((available + on_order) / avg)` · `suggested_qty = max(0, ceil(avg × (lead_time + cover_days)) − available − on_order)` round-up MOQ NCC mặc định · urgency `urgent` (≤ lead) / `soon` (≤ lead+cover) / `ok` (ẩn). Gợi ý NCC default từ `supplier_prices.is_default=true`.
- ✅ `createPoFromSuggestions(rows)`: chọn nhiều dòng → BE chia theo `supplier_id` → **1 PO `draft` per NCC**, FE redirect `/procurement/purchase-orders` để rà soát + confirm.
- ✅ REST: `GET /procurement/demand-planning?window_days=&lead_time=&cover_days=&urgency=&supplier_id=&q=` + `POST /procurement/demand-planning/create-po`. Test `DemandPlanningTest` (5 ca: velocity + lead time, urgent threshold, on_order trừ suggestion, create PO chia theo NCC, MOQ round up).
- ✅ FE `/procurement/demand-planning` — Statistic 4 ô (Tổng SKU / Urgent / Soon / Tổng giá trị đề xuất) + bộ lọc + bảng (SKU/Mức độ/Bán/ngày/Tồn/Đang về/Số ngày còn hàng + Progress màu/Đề xuất nhập editable/NCC gợi ý/Thành tiền) + Bulk "Tạo PO nháp (N)". Sidebar "Mua hàng → Đề xuất nhập hàng".

**Phase 6.4 — Billing SaaS  ◑ (đang triển khai · spec [`docs/specs/0018-billing-saas.md`](../specs/0018-billing-saas.md)):**
- Subscription plan (4 gói: `trial · starter · pro · business`) + hạn mức **số gian hàng kết nối** (2/5/10, không giới hạn số đơn) + gating tính năng nâng cao (`procurement`/`fifo_cogs`/`profit_reports`/`finance_settlements`/`demand_planning`/`mass_listing`/`automation_rules`) qua middleware `EnforcePlanLimit`/`EnforcePlanFeature` (`402 PLAN_LIMIT_REACHED`/`PLAN_FEATURE_LOCKED`); dùng thử **14 ngày** auto-start khi tạo tenant; thanh toán **SePay** (chuyển khoản tự động qua webhook sao kê) + **VNPay** (redirect + IPN, HMAC-SHA512); **MoMo** skeleton. Hết hạn ⇒ grace 7 ngày → auto rớt về `trial` vĩnh viễn (không khoá data — cam kết "Dữ liệu của bạn là của bạn"). Pattern `PaymentGatewayConnector` + `PaymentRegistry` (mirror `ChannelRegistry`/`CarrierRegistry`). Bảng: `plans · subscriptions · invoices(+invoice_lines) · payments · usage_counters · billing_profiles`.

**Phase 6.5 — Automation Rules + Notifications  ◑ (đang triển khai):**
- Rules engine (tự ghép SKU theo regex, tự tag đơn theo điều kiện, auto-block khách reputation thấp, auto-create PO định kỳ) · Thông báo Zalo OA / Email / In-app cho hết tồn / đơn lỗi / settlement bất thường.
- ✅ **Nền tảng kênh `mail`** (SPEC 0022 · 2026-05-16): module `Notifications` mới + 3 email cốt lõi (`VerifyEmailNotification`, `WelcomeNotification`, `ResetPasswordNotification`) qua queue `notifications`; `User implements MustVerifyEmail`; middleware `verified` JSON-envelope gắn vào group tenant (hard gating `403 EMAIL_NOT_VERIFIED`); template Blade branded `CMBcoreSeller` (1 layout + 3 view, inline CSS email-safe, responsive 600px); 4 endpoint mới `/auth/email/verify/*` + `/auth/password/*` với throttle chống brute & enumerate.
- ✅ **Admin operator toolbox** (SPEC 0023 · 2026-05-16): vouchers (4 kind: percent/fixed/free_days/plan_upgrade) + custom trial N ngày + plan editor (sửa limits/features/price không re-deploy) + per-tenant feature override + manual invoice & mark-paid (offline payment) + payment refund (optional rollback subscription) + audit log search cross-tenant + broadcast email tới owners/tenant_ids qua queue `notifications`. 4 trang admin mới `/admin/{vouchers,plans,audit-logs,broadcasts}`.
- ☐ Còn lại: Rules engine; notification cho event nghiệp vụ (hết tồn / đơn lỗi / settlement bất thường); channels Zalo OA / Telegram / In-app / Web push; notification preferences UI.

## Phase 7 — Kế toán đầy đủ (VAS TT133)  ✅ (Implemented 2026-05-15 — toàn bộ 5 sub-phase; SPEC-0019; 46/46 Accounting tests xanh)

**Bối cảnh:** sau Phase 6, app đã có **đối soát phí sàn thực** (Finance `settlements`), **FIFO COGS** (`order_costs`), **PO/NCC** (Procurement), **báo cáo lợi nhuận** (Reports). Đó là nguồn dữ liệu **đầu vào** đủ tốt cho một module kế toán chuyên nghiệp ghi sổ kép, lập BCTC, theo dõi công nợ, đối chiếu ngân hàng. Module mới **`Accounting`** (đứng ngang `Finance`, không thay thế) sẽ **lắng nghe domain event** từ các module hiện có và tự ghi sổ — đúng luật phụ thuộc `modules.md` §3.

**Quyết định nền (chốt 2026-05-15 với chủ dự án):**
1. **CoA = TT133** (DN nhỏ & vừa — seed sẵn 1 template; mở rộng vài TK TT200 khi cần).
2. **Chỉ VND** — không cột FX, không multi-currency (đúng `vision-and-scope.md` §4).
3. **Năm tài chính = năm dương lịch** (1/1–31/12), không configurable.
4. **Hoãn HĐĐT** — đẩy sang Phase 8+ backlog. Phase 7 không có HĐĐT.
5. **Export ra phần mềm KT ngoài** — CSV/Excel theo schema **MISA AMIS** (file để user tự import), không tích hợp API ngoài.
6. **Mapping CoA cho auto-post lưu DB**, tenant chỉnh qua UI Settings (`accounting_post_rules`).
7. **Triển khai cuốn chiếu** 7.1 → 7.2 → 7.3 → 7.4 → 7.5, mỗi sub-phase 1 SPEC độc lập.

**Phase 7.1 — Accounting Foundation  ☐ (spec `docs/specs/0019-accounting-foundation.md`):**
- Bảng `chart_accounts` (CoA TT133 seeded), `fiscal_periods` (tháng/quý/năm, status `open|closed|locked`), `journal_entries` (immutable, idempotency_key unique, source_module/source_id), `journal_lines` (partition `posted_at` theo tháng, check `Σ Dr = Σ Cr`), `account_balances` (materialized).
- `JournalService::post(JournalEntryDTO)` — duy nhất nơi ghi sổ; check kỳ chưa locked; idempotent qua `idempotency_key`.
- `PeriodService::close/reopen/lock`; lock kỳ ⇒ bút toán mới chỉ được post vào kỳ mở kế tiếp với `narration="Điều chỉnh kỳ X"`.
- `BalanceService::recompute(period)` — idempotent rebuild `account_balances`.
- Listener nền: `PostOnGoodsReceiptConfirmed` (Dr 156/Cr 331), `PostOnInventoryTransfer` (Dr 156-to/Cr 156-from theo `dim_warehouse`), `PostOnStocktakeAdjust` (Dr 156/Cr 711 hoặc Dr 811/Cr 156).
- UI shell: `/accounting/chart-of-accounts` (tree), `/accounting/journals` (sổ NK chung, modal tạo bút toán tay), `/accounting/periods`, `/settings/accounting/post-rules` (mapping editor).
- RBAC: `accounting.view|post|close_period|config|export` (mặc định cấp owner/admin/accountant; staff_* không có gì).
- Billing gating: feature `accounting_basic` (Pro + Business). Plan thấp ⇒ `402 PLAN_FEATURE_LOCKED`.
- ADR-0011 (double-entry), ADR-0012 (TT133 seed), ADR-0013 (event-driven posting), ADR-0014 (period lock), ADR-0016 (partition `journal_lines`).

**Phase 7.2 — AR (Công nợ phải thu)  ☐:**
- Listener `PostOnOrderShipped` ⇒ Dr 131(customer)/Cr 511 + Cr 33311(VAT đầu ra). `PostOnOrderCancelled/Returned` ⇒ entry đảo (`is_reversal_of_id`). `PostOnSettlementReconciled` ⇒ Dr 642/635 (phí sàn) + Dr 112 (payout)/Cr 131 — khép vòng đời đơn.
- Bảng `customer_receipts` (phiếu thu COD/CK) cho đơn manual không qua sàn payout.
- Sổ chi tiết 131 group theo `customer_id` (re-use bảng `customers`); báo cáo Aged AR (0–30, 31–60, 61–90, >90).
- UI: `/accounting/ar` (Aging table + drawer chi tiết khách + nút "Tạo phiếu thu").

**Phase 7.3 — AP (Công nợ phải trả NCC)  ☐:**
- `vendor_bills` (hoá đơn NCC, link `po_id?`/`goods_receipt_id?`) + `vendor_payments` (phiếu chi).
- Listener `PostOnGoodsReceiptConfirmed` mở rộng: nếu shop bật auto-bill ⇒ tự tạo `vendor_bill draft` từ GR + post Dr 156/Dr 133/Cr 331.
- Sổ chi tiết 331 group theo `supplier_id` (re-use `suppliers`); Aged AP.
- UI: `/accounting/ap` (Aging + drawer NCC + tab "Hoá đơn NCC" + nút "Tạo phiếu chi" + applied_bills jsonb).

**Phase 7.4 — Quỹ & Ngân hàng  ☐:**
- `cash_accounts` (kind `cash|bank|ewallet|cod_intransit`, 1-1 `gl_account_id` ↔ TK 1111/1121/…), `cash_movements` (sổ quỹ chi tiết, song song journal entry).
- `bank_statements` + `bank_statement_lines` (import CSV/MT940/**webhook SePay** đã có sẵn từ SPEC-0018 — re-use). Matching wizard: left = bank line, right = candidate movement/settlement payout.
- UI: `/accounting/cash` (sổ quỹ + sổ ngân hàng + import sao kê + matching screen).

**Phase 7.5 — VAT + Báo cáo tài chính + Export MISA  ☐:**
- `tax_codes` (VAT0/5/8/10/exempt), `tax_filings` (`01/GTGT-YYYY-MM`, draft → submitted).
- Báo cáo: **Bảng cân đối số phát sinh**, **Báo cáo KQKD (B02-DN nhỏ)**, **Bảng cân đối kế toán (B01-DN nhỏ)**, **Lưu chuyển tiền tệ (B03-DN nhỏ)**, **Sổ nhật ký chung**, **Sổ cái** + **Sổ chi tiết TK**, **Aged AR/AP**.
- Export: Excel multi-sheet (Sổ NK + danh mục TK + chi tiết phát sinh) theo **schema MISA AMIS** (file user tự import); PDF qua Gotenberg.
- UI: `/accounting/reports` (5 tab + filter kỳ/TK/đối tượng + nút Export), `/accounting/tax` (danh sách tờ khai + preview).
- Billing gating: feature `accounting_advanced` (chỉ Business — VAT + tờ khai + đối soát NH + export MISA).

**Phase 7+ — Backlog kế toán:** phê duyệt nhiều cấp cho bút toán tay; chi phí vận hành & phân bổ (payroll lite, marketing, allocation rule); tích hợp **API** MISA AMIS / Fast / SmartBooks (thay vì chỉ file Excel); audit log trail riêng cho kế toán.

## Phase 8+ — Hậu mãi & nâng cao  ☐
Trả hàng/hoàn tiền; chat hợp nhất đa sàn; **HĐĐT (TT78 — VNPT/Viettel/MISA + ký số)** (đẩy từ Phase 7+ cũ); tối ưu hiệu năng (read replica, search engine, archive partition); realtime UI (Reverb); PWA quét đóng gói.

---

## Backlog (chưa xếp phase — đừng làm cho tới khi được xếp)
- Tích hợp nguồn hàng 1688/Taobao · POS bán tại quầy · **CRM marketing đầy đủ** (campaign, gửi tin Zalo/SMS — *không* nhầm với "Sổ khách hàng" ở Phase 2, vốn chỉ là CRM nội bộ cho vận hành đơn) · đa quốc gia · API public cho bên thứ ba · marketplace plugin · gửi tin xác nhận đơn qua Zalo OA/SMS.
