<?php

/*
|--------------------------------------------------------------------------
| VisualSearch routes (/api/v1/visual-search/*)
|--------------------------------------------------------------------------
|
| CRUD item + upload ảnh + lookup (tìm bằng ảnh) thêm ở phase HTTP.
| Để trống có chủ đích — provider vẫn loadRoutesFrom an toàn.
|
*/
